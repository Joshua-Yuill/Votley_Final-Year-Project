question = {
    title: "A title",
    answers: [
        "option 1",
        "option 2"
    ], 
}

// answer index : votes counter
responses = {
    0: 0,
    1: 0,
}

responses = defaultdict(default=0)
responses[2] += 1

responses.keys()

// Chart.js for chart drawing maybe? need to look into it more